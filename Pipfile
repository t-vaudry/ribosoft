[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
click = ">=8.1,<9.0"
click-log = ">=0.4,<0.5"
requests = ">=2.31,<3.0"
urllib3 = ">=2.0,<3.0"
aiohttp = ">=3.9,<4.0"
aiofiles = ">=23.0,<24.0"

[dev-packages]
pytest = ">=8.0"
pytest-cov = ">=5.0"
pytest-asyncio = ">=0.23"
black = ">=24.0"
mypy = ">=1.8"
ruff = ">=0.3"
pre-commit = "*"

[requires]
python_version = "3.13"

[scripts]
test = "pytest"
lint = "ruff check ."
format = "black ."
typecheck = "mypy ."
